# 訂單系統改造方案總覽（基於現有代碼）

## 📌 改造原則

### ✅ 保留不動的部分
- `__init__.py` - 現有所有路由和邏輯
- `models.py` - 現有數據庫函數
- `tracking.js` (5473行) - 保持原樣
- `tracking.css` (4739行) - 保持原樣
- `STATUS_SYSTEM.js` - 保持原樣
- 所有現有的 HTML 模板

### ➕ 新增的部分
- 新的數據表（products, files, notifications 等）
- 新的 API 路由（在 __init__.py 中添加）
- 新的 JS 文件（放在 static/js/modules/）
- 新的 CSS 文件（放在 static/css/modules/）
- 新的 HTML 模板

---

## 🗂️ 10 個改造模塊

### **M0 - 項目準備與結構調整**
**時間**: 0.5 天  
**依賴**: 無

**目標**:
- 創建新文件夾結構
- 理解現有代碼
- 不修改任何現有文件

**內容**:
- 創建 static/js/modules/
- 創建 static/css/modules/
- 創建 templates/tracking/products/
- 準備數據庫遷移腳本

---

### **M1 - 用戶角色擴充（Staff 業務員）**
**時間**: 0.5 天  
**依賴**: M0

**目標**:
- 在現有 users 表添加 real_name 欄位
- 添加 staff 角色
- 創建業務員帳號管理功能

**修改的文件**:
- `models.py` - 添加一個遷移函數
- `__init__.py` - 添加幾個新路由
- 新增 `static/js/modules/user-management.js`
- 新增 `templates/tracking/admin/users.html`

**不動的部分**:
- 現有的 login_required、admin_required 裝飾器
- 現有的登入邏輯

---

### **M2 - 產品層級（核心改造）**
**時間**: 2-3 天  
**依賴**: M1

**目標**:
- 創建 products 表（雙層結構）
- 創建 product_status_history 表
- 實現產品 CRUD API
- 產品列表前端

**新增的數據表**:
```sql
CREATE TABLE products (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    product_id VARCHAR(20) UNIQUE,        -- PR001, PR002
    order_number VARCHAR(50),             -- 關聯到 orders.order_number
    product_name VARCHAR(200),
    product_code VARCHAR(50),
    quantity VARCHAR(50),
    factory VARCHAR(100),
    production_type VARCHAR(100),
    expected_delivery_date DATE,
    
    current_status VARCHAR(50),
    status_updated_at TIMESTAMP,
    status_days INTEGER DEFAULT 0,
    
    created_by_id INTEGER,                -- 創建者
    handler_id INTEGER,                   -- 當前負責人（業務員）
    
    folder_path VARCHAR(500),             -- /uploads/訂單號/產品ID/
    notes TEXT,
    
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (created_by_id) REFERENCES users(id),
    FOREIGN KEY (handler_id) REFERENCES users(id)
);

CREATE TABLE product_status_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    product_id VARCHAR(20),
    order_number VARCHAR(50),
    from_status VARCHAR(50),
    to_status VARCHAR(50),
    action_date DATE,
    operator_id INTEGER,
    notes TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (operator_id) REFERENCES users(id)
);
```

**新增的 API**（在 __init__.py 中添加）:
- `POST /api/orders` - 主管創建訂單
- `POST /api/orders/<order_number>/products` - 業務員創建產品
- `GET /api/orders/<order_number>/products` - 產品列表（權限過濾）
- `PUT /api/products/<product_id>/status` - 更新產品狀態
- `GET /api/products/<product_id>` - 產品詳情

**新增的文件**:
- `static/js/modules/product-manager.js` - 產品管理邏輯
- `static/css/modules/products.css` - 產品樣式
- `templates/tracking/products/list.html` - 產品列表頁

---

### **M3 - 檔案管理系統**
**時間**: 1-2 天  
**依賴**: M2

**目標**:
- 實現檔案上傳到產品
- 檔案下載
- 檔案軟刪除
- 批量刪除

**新增的數據表**:
```sql
CREATE TABLE files (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    file_id VARCHAR(20) UNIQUE,           -- F00001, F00002
    order_number VARCHAR(50),
    product_id VARCHAR(20),
    
    filename VARCHAR(500),
    file_path VARCHAR(1000),
    file_size INTEGER,
    file_type VARCHAR(100),
    
    uploaded_by_id INTEGER,
    uploaded_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    is_deleted BOOLEAN DEFAULT 0,
    deleted_by_id INTEGER,
    deleted_at TIMESTAMP,
    
    FOREIGN KEY (uploaded_by_id) REFERENCES users(id),
    FOREIGN KEY (deleted_by_id) REFERENCES users(id)
);
```

**新增的 API**:
- `POST /api/products/<product_id>/files` - 上傳檔案
- `GET /api/products/<product_id>/files` - 檔案列表
- `GET /api/files/<file_id>/download` - 下載
- `DELETE /api/files/<file_id>` - 刪除
- `POST /api/files/batch-delete` - 批量刪除

**新增的文件**:
- `static/js/modules/file-uploader.js`
- `static/css/modules/file-upload.css`

---

### **M4 - 產品轉移功能**
**時間**: 1 天  
**依賴**: M2

**目標**:
- 主管可以轉移產品給其他業務員
- 記錄轉移歷史

**新增的數據表**:
```sql
CREATE TABLE product_handover_log (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    product_id VARCHAR(20),
    order_number VARCHAR(50),
    
    from_handler_id INTEGER,
    to_handler_id INTEGER,
    handover_by_id INTEGER,               -- 操作人（主管）
    
    handover_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    reason TEXT,
    
    FOREIGN KEY (from_handler_id) REFERENCES users(id),
    FOREIGN KEY (to_handler_id) REFERENCES users(id),
    FOREIGN KEY (handover_by_id) REFERENCES users(id)
);
```

**新增的 API**:
- `POST /api/admin/products/<product_id>/transfer`
- `POST /api/admin/products/batch-transfer`
- `GET /api/admin/products/<product_id>/handover-history`

**新增的文件**:
- `static/js/modules/product-transfer.js`

---

### **M5 - 操作日誌系統**
**時間**: 1 天  
**依賴**: M2, M4

**目標**:
- 記錄關鍵操作（創建訂單、轉移產品等）
- 主管可查看日誌

**新增的數據表**:
```sql
CREATE TABLE operation_logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,
    
    operation_type VARCHAR(50),           -- create_order, transfer_product
    operation_desc VARCHAR(500),
    
    order_number VARCHAR(50),
    product_id VARCHAR(20),
    target_user_id INTEGER,
    
    details TEXT,                         -- JSON
    ip_address VARCHAR(50),
    user_agent VARCHAR(500),
    
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

**工具函數**（在 models.py 中添加）:
```python
def log_operation(user_id, operation_type, operation_desc, **kwargs):
    """記錄操作日誌"""
    pass
```

**新增的 API**:
- `GET /api/admin/logs` - 查詢日誌

**新增的文件**:
- `static/js/modules/operation-logs.js`

---

### **M6 - 通知系統**
**時間**: 1-2 天  
**依賴**: M2, M4

**目標**:
- 站內通知
- 定時檢查逾期訂單
- 通知鈴鐺 UI

**新增的數據表**:
```sql
CREATE TABLE notifications (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,
    
    type VARCHAR(50),                     -- order_created, product_transferred
    title VARCHAR(200),
    message TEXT,
    
    order_number VARCHAR(50),
    product_id VARCHAR(20),
    
    is_read BOOLEAN DEFAULT 0,
    read_at TIMESTAMP,
    
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

**工具函數**（在 models.py 中添加）:
```python
def send_notification(user_ids, notification_type, title, message, **kwargs):
    """發送通知"""
    pass

def check_overdue_products():
    """定時任務：檢查逾期產品"""
    pass
```

**新增的 API**:
- `GET /api/notifications`
- `POST /api/notifications/<id>/read`
- `POST /api/notifications/read-all`

**新增的文件**:
- `static/js/modules/notifications.js`
- `static/css/modules/notification-bell.css`

---

### **M7 - 數據匯出功能**
**時間**: 1 天  
**依賴**: M2, M3

**目標**:
- 匯出訂單列表（Excel）
- 匯出產品列表（Excel）
- 批量下載檔案（ZIP）
- 客戶名稱自動建議

**新增的 API**:
- `GET /api/admin/export/orders` - 匯出訂單 Excel
- `GET /api/admin/export/products` - 匯出產品 Excel
- `GET /api/orders/<order_number>/download-all` - 下載所有檔案 ZIP
- `GET /api/customers/suggestions` - 客戶名稱列表

**新增的文件**:
- `static/js/modules/data-export.js`

---

### **M8 - 圖片管理 UI（三欄布局）**
**時間**: 2-3 天  
**依賴**: M2, M3

**目標**:
- 右側抽屜展開成三欄
- 左：訂單切換 + 產品列表
- 中：大圖預覽 + 縮圖
- 右：訂單詳情（保留原有）
- 圖片縮放、旋轉、全螢幕

**新增的 API**:
- `GET /api/orders/adjacent` - 獲取相鄰訂單（用於切換）
- `GET /api/orders/<order_number>/full` - 完整訂單數據（訂單+產品+檔案）

**新增的文件**:
- `static/js/modules/image-viewer.js` - 圖片查看器
- `static/js/modules/three-column-layout.js` - 三欄布局邏輯
- `static/css/modules/image-viewer.css`
- `static/css/modules/three-columns.css`

---

### **M9 - 主管儀表板**
**時間**: 1-2 天  
**依賴**: M2, M5, M6

**目標**:
- 總覽統計
- 紅黃綠燈分布
- 業務員工作量
- 最近活動

**新增的 API**:
- `GET /api/admin/dashboard` - 儀表板數據

**新增的文件**:
- `templates/tracking/admin/dashboard.html`
- `static/js/modules/dashboard.js`
- `static/css/modules/dashboard.css`

---

### **M10 - 代碼重構指南（可選）**
**時間**: 2-3 天  
**依賴**: M1-M9 全部完成

**目標**:
- 拆分 tracking.js (5473行)
- 拆分 tracking.css (4739行)
- 這個模塊是可選的，可以改造完成後再決定要不要做

---

## 📊 總時間估算

| 模塊 | 時間 | 類型 |
|------|------|------|
| M0 | 0.5天 | 準備 |
| M1 | 0.5天 | 用戶 |
| M2 | 2-3天 | ⭐核心 |
| M3 | 1-2天 | 檔案 |
| M4 | 1天 | 轉移 |
| M5 | 1天 | 日誌 |
| M6 | 1-2天 | 通知 |
| M7 | 1天 | 匯出 |
| M8 | 2-3天 | ⭐UI |
| M9 | 1-2天 | 儀表板 |
| M10 | 2-3天 | 可選 |

**總計**: 12-20 天（不含 M10）

---

## 🎯 實施順序建議

### **第 1 週：核心功能**
- Day 1: M0 + M1
- Day 2-4: M2（產品層級）
- Day 5: M3（檔案管理）

### **第 2 週：擴展功能**
- Day 6: M4（產品轉移）
- Day 7: M5（操作日誌）
- Day 8-9: M6（通知系統）
- Day 10: M7（數據匯出）

### **第 3 週：UI 和收尾**
- Day 11-13: M8（圖片管理 UI）
- Day 14-15: M9（主管儀表板）
- Day 16-18: 測試和修復

### **（可選）第 4 週：重構**
- Day 19-21: M10（代碼重構）

---

## ✅ 改造完成後的系統架構

### **數據庫表**:
```
現有表：
├── users (擴充 real_name)
├── orders (保持不變)
├── status_history (保持不變)
├── notes (保持不變)
├── revisions (保持不變)
└── audit_log (保持不變)

新增表：
├── products ⭐
├── product_status_history ⭐
├── files ⭐
├── product_handover_log
├── operation_logs
└── notifications
```

### **文件結構**:
```
order_tracking/
├── __init__.py (現有 + 新增路由)
├── models.py (現有 + 新增函數)
├── config.py (保持不變)
├── status_config.py (保持不變)
├── status_definitions.py (保持不變)
├── static/
│   ├── js/
│   │   ├── tracking.js (保持不變 5473行)
│   │   ├── STATUS_SYSTEM.js (保持不變)
│   │   ├── new_order.js (保持不變)
│   │   └── modules/ (新增)
│   │       ├── product-manager.js
│   │       ├── file-uploader.js
│   │       ├── image-viewer.js
│   │       ├── notifications.js
│   │       └── ...
│   └── css/
│       ├── tracking.css (保持不變 4739行)
│       ├── login.css (保持不變)
│       └── modules/ (新增)
│           ├── products.css
│           ├── file-upload.css
│           ├── image-viewer.css
│           └── ...
└── templates/
    └── tracking/
        ├── base.html (可能小幅修改，加載新 JS/CSS)
        ├── index.html (保持不變)
        ├── order_detail.html (保持不變)
        ├── products/ (新增)
        │   └── list.html
        └── admin/ (新增)
            ├── users.html
            └── dashboard.html
```

---

## 🔧 給 Cursor 的使用方式

### **方式 1：逐個模塊餵給 Cursor**

```
Day 1:
你：「請閱讀 M0-項目準備.md 並執行」
Cursor：創建新文件夾...

Day 2:
你：「M0 完成了，現在請閱讀 M1-用戶角色擴充.md」
Cursor：添加 real_name 欄位...
```

### **方式 2：每個模塊內分步驟**

```
M2 第一步：
你：「請在 models.py 中添加 products 表的創建函數」

M2 第二步：
你：「請在 __init__.py 中添加產品 API 路由」

M2 第三步：
你：「請創建 product-manager.js」
```

---

## 📝 重要提醒

### ✅ 要做的：
- 嚴格按照模塊順序（有依賴關係）
- 每個模塊完成後測試
- Git commit 每個模塊
- 新功能寫在新文件

### ❌ 不要做的：
- 不要修改 tracking.js (5473行)
- 不要修改 tracking.css (4739行)
- 不要重構現有的 __init__.py 邏輯
- 不要改動現有的模板（除非必要）

---

## 🎉 改造完成的標誌

- ✅ 主管可以創建訂單
- ✅ 業務員可以創建產品
- ✅ 業務員只看到自己的產品
- ✅ 檔案可以上傳下載
- ✅ 主管可以轉移產品
- ✅ 通知系統正常工作
- ✅ 圖片管理 UI 可用
- ✅ 主管儀表板顯示正常
- ✅ 所有現有功能正常（沒有被破壞）

**準備好開始了！** 🚀
